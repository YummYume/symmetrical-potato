<!DOCTYPE html>
<html class="h-full" lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ site }}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 h-full">
        <main class="grid min-h-full place-items-center">
            <div class="
                bg-white max-w-7xl min-w-96 mx-auto overflow-clip ring-1
                ring-slate-900/10 rounded-lg py-6 sm:px-6 lg:px-8
            ">
                {% block title %}
                    {% if title is defined %}
                        <div class="border-b border-gray-200 bg-white px-4 py-5 sm:px-6">
                            <h1 class="text-3xl font-extrabold tracking-tight text-slate-900 text-center">
                                {{ title }} !
                            </h1>
                        </div>
                    {% endif %}
                {% endblock %}

                <div class="px-4 py-4 sm:px-6">
                    {% block content %}
                        {% if text is defined %}
                            <p>{{ text }}</p>
                        {% endif %}
                    {% endblock %}
                </div>
            </div>
        </main>
    </body>
</html>
